#include <stdio.h>

int main() {
    int a[] = {1, 2, 3, 4, 5};

    for(int i = 0; i < 5 / 2; i++) {
        int temp = a[i];
        a[i] = a[4 - i];
        a[4 - i] = temp;
    }

    for(int i = 0; i < 5; i++)
        printf("%d ", a[i]);

    return 0;
}





Prediction
5 4 3 2 1
